{
  "name": "greenlight-ai-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Browser tests over both apps against the seeded stack and the scripted model.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
