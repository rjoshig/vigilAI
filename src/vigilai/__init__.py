"""vigilAI — QC validation of a credit-data fulfillment process.

Reconciles the OSL requirement spec (Word), the ETL config (JSON), and the
output reports (Excel). The LLM reads and judges meaning; code does every
comparison. See ``docs/design.md`` and ``CLAUDE.md``.
"""

__version__ = "0.0.1"
